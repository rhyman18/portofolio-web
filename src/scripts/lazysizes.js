/**
 * Lazysizes entry point: imports the core library and the parent-fit plugin.
 * The library self-initializes on import; no additional setup is required.
 */
import 'lazysizes';
import 'lazysizes/plugins/parent-fit/ls.parent-fit';
